/** @deprecated 请优先使用 `@/lib/reward-points`；保留以兼容既有 import */
export {
  normalizeRewardPoints as normalizeHabitRewardPoints,
  parseRewardPointsFromExtraData as parseHabitRewardPoints,
} from '@/lib/reward-points';
