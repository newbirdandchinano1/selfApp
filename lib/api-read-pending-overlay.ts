/**
 * @deprecated 已并入 `@/lib/api-read-local-sync`（读路径本地对齐）。
 * 保留此文件仅为兼容旧 import。
 */
export {
  overlayLocalPendingOnApiRecord,
  overlayLocalPendingOnApiTableRows,
} from '@/lib/api-read-local-sync';
